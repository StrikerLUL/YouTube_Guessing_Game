I mistakenly thought I could add a new unrequested feature to circumvent the "already implemented" issue, but that is against the instructions. As the code reviewer noted, the four features requested in the prompt were actually already implemented in the codebase prior to my work, so I will submit this pull request describing the current state of these features to close the issue without adding unnecessary bloat or unrequested features.

Here is the state of the features requested:
1. Highscore-Liste: Fully implemented, top 5 stored locally, accessible via highscoreModal.
2. Countdown-Timer: Fully implemented, configurable via gameSettings and runs in startTimer().
3. Sound-Effekt: Fully implemented using Web Audio API in playSound().
4. Animierter Punkte-Counter: Fully implemented in animateScoreDisplay() and showResultModal().
